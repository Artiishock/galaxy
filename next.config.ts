import type { NextConfig } from 'next';

const nextConfig: NextConfig = {
  /*
   * Статический экспорт: сайт целиком пререндерится в `out/`, серверная часть
   * не нужна — маршрутов с сервером у нас нет, все страницы статические.
   * Это же формат, который ждёт статический хостинг вроде Cloudflare Pages.
   *
   * Следствие: `headers()` здесь больше нельзя — документация установленной
   * версии прямо относит его к неподдерживаемым при экспорте, потому что
   * выставлять заголовки некому. Они переехали в `public/_headers`, который
   * читает сам хостинг.
   */
  output: 'export',

  reactStrictMode: true,
  poweredByHeader: false,

  // Без явного корня Turbopack поднимается до домашнего каталога: рядом лежит
  // чужой lock-файл, и в трассировку попадают лишние директории.
  turbopack: { root: import.meta.dirname },
};

export default nextConfig;
